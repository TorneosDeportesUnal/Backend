<p id="notice"><%= notice %></p>

<h1>Modalities</h1>

<table>
  <thead>
    <tr>
      <th>Discipline</th>
      <th>Gender</th>
      <th>Tournament</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @modalities.each do |modality| %>
      <tr>
        <td><%= modality.discipline %></td>
        <td><%= modality.gender %></td>
        <td><%= modality.tournament_id %></td>
        <td><%= link_to 'Show', modality %></td>
        <td><%= link_to 'Edit', edit_modality_path(modality) %></td>
        <td><%= link_to 'Destroy', modality, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Modality', new_modality_path %>
