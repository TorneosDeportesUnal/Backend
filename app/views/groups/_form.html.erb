<%= form_for(group) do |f| %>
  <% if group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :winners_number %>
    <%= f.number_field :winners_number %>
  </div>

  <div class="field">
    <%= f.label :tournament_phase_id %>
    <%= f.text_field :tournament_phase_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
