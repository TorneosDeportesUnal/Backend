<%= form_for(availavility) do |f| %>
  <% if availavility.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(availavility.errors.count, "error") %> prohibited this availavility from being saved:</h2>

      <ul>
      <% availavility.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :day_of_week %>
    <%= f.date_select :day_of_week %>
  </div>

  <div class="field">
    <%= f.label :begin_hour %>
    <%= f.datetime_select :begin_hour %>
  </div>

  <div class="field">
    <%= f.label :end_hour %>
    <%= f.datetime_select :end_hour %>
  </div>

  <div class="field">
    <%= f.label :team_id %>
    <%= f.text_field :team_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
