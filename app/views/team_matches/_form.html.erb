<%= form_for(team_match) do |f| %>
  <% if team_match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(team_match.errors.count, "error") %> prohibited this team_match from being saved:</h2>

      <ul>
      <% team_match.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :outcome %>
    <%= f.number_field :outcome %>
  </div>

  <div class="field">
    <%= f.label :points_gained %>
    <%= f.number_field :points_gained %>
  </div>

  <div class="field">
    <%= f.label :goals %>
    <%= f.number_field :goals %>
  </div>

  <div class="field">
    <%= f.label :match_id %>
    <%= f.text_field :match_id %>
  </div>

  <div class="field">
    <%= f.label :team_id %>
    <%= f.text_field :team_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
