<p id="notice"><%= notice %></p>

<h1>Team Matches</h1>

<table>
  <thead>
    <tr>
      <th>Outcome</th>
      <th>Points gained</th>
      <th>Goals</th>
      <th>Match</th>
      <th>Team</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @team_matches.each do |team_match| %>
      <tr>
        <td><%= team_match.outcome %></td>
        <td><%= team_match.points_gained %></td>
        <td><%= team_match.goals %></td>
        <td><%= team_match.match %></td>
        <td><%= team_match.team %></td>
        <td><%= link_to 'Show', team_match %></td>
        <td><%= link_to 'Edit', edit_team_match_path(team_match) %></td>
        <td><%= link_to 'Destroy', team_match, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Team Match', new_team_match_path %>
