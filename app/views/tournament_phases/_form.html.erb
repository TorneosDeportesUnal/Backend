<%= form_for(tournament_phase) do |f| %>
  <% if tournament_phase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tournament_phase.errors.count, "error") %> prohibited this tournament_phase from being saved:</h2>

      <ul>
      <% tournament_phase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phase_type %>
    <%= f.text_field :phase_type %>
  </div>

  <div class="field">
    <%= f.label :tournament_id %>
    <%= f.text_field :tournament_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
